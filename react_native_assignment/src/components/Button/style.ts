import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    borderRadius: 10,
    backgroundColor: "#ffd966",
    margin: 10,
    shadowOpacity: 0.2,
  },
  text: {
    padding: 10,
    textAlign: "center",
    color: "white",
  },
});
